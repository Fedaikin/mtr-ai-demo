# Review: codex/individual-demo-credential

## 1. Паспорт ветки

- Ветка: `codex/individual-demo-credential`.
- Назначение: создать техническую возможность для отдельной демонстрационной учётной записи без ротации общего Production-пароля.
- Base SHA: `02e3cdfbc1553fb9731bb816487aaa7af7a9e153`.
- Проверяемый commit: `6ecc528871d304dd4b43c1a7cbc6137e9324be9d`.

## 2. Scope и traceability

- [x] Изменены только server-side authentication, seed-readiness gate и интеграционный regression test.
- [x] UI, RBAC-матрица, сценарии, спецификации, каталоги, чаты и Production-данные этим commit не меняются.
- [x] Новая ветка не публикует plaintext-пароль, хеш или секрет в Git.

## 3. Git и интеграция

- [x] Рабочее дерево было чистым до изменения.
- [x] `git diff --check` — PASS.
- [x] Нет force-push, переписывания `main` или удаления чужих веток.

## 4. Архитектура и границы модулей

- [x] Исключение для отдельной учётной записи ограничено `auth_source='DEMO_INDIVIDUAL'` и не меняет поведение обычных demo-персон.
- [x] `AuthUserRecord` дополнен только server-side полем `authSource`; оно не передаётся в клиентский DTO.
- [x] Readiness gate допускает дополнительные учётные записи при сохранении обязательного базового набора из восьми субъектов.

## 5. RBAC и авторизация

- [x] Права не выводятся из логина или запроса пользователя: они остаются в project memberships, role assignments и source claims.
- [x] Новая учётная запись будет создана отдельной транзакцией как копия доступов `demo`; это не расширяет полномочия существующих субъектов.
- [x] Service accounts и неактивные пользователи по-прежнему не могут пройти интерактивный вход.

## 6. Данные, SQL и миграции

- [x] Миграции и схема не менялись.
- [x] Изменение readiness gate не делает seed разрушительным при наличии добавленного администрацией пользователя.
- [x] Production-вставка пользователя выполняется после merge отдельно, одной транзакцией с последующей проверкой состава доступов.

## 7. МТР-процессы

- [x] Н/П: алгоритмы МТР, позиции, аналоги, запуски, отчёты и KPI не затронуты.

## 8. AI-агент и LLM trust boundary

- [x] Н/П: модель, prompts, connectors, доказательства, tool calls и режим `PROPOSE_ONLY` не затронуты.

## 9. Role-aware UI и пользовательские сценарии

- [x] Н/П: экран и клиентский bundle не получают новый пароль или auth-source.
- [x] Вход новой учётной записи проходит через существующий `/api/auth/login` с теми же сессиями HttpOnly.

## 10. Privacy, security и аудит

- [x] Новый plaintext используется только как runtime input при операционном создании записи; в Git, UI, логи и артефакты не записывается.
- [x] В БД сохраняется только scrypt-хеш индивидуального пароля.
- [x] Общий `DEMO_PASSWORD_HASH` не меняется, поэтому существующие demo-персоны и их активные сессии не затрагиваются.

## 11. Тесты

- [x] `pnpm vitest run tests/integration/auth-session.test.ts` — PASS, 10/10.
- [x] Новый regression: отдельный пароль принимается у `DEMO_INDIVIDUAL`, но общий пароль не подходит этой учётной записи.
- [x] `pnpm typecheck` — PASS.
- [x] `pnpm lint` — PASS.

## 12. Производительность и Vercel

- [x] Изменение выполняется только во время login, без дополнительного browser round-trip или client bundle.
- [x] После merge требуется Production deployment и HTTP-проверка входа новой учётной записи.

## 13. Итог

- [x] ГОТОВО К MERGE: применимые проверки пройдены; Production-данные до merge не менялись.
- Rollback: revert этого commit; новая учётная запись при необходимости удаляется точечно со своими сессиями, assignments, memberships и claims.
